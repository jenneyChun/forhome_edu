# MagicSquare_1004 — TDD RED 단계 — validate_lines T2 행·열 합 실패 테스트 추가

| 항목 | 내용 |
|------|------|
| **프로젝트** | MagicSquare_1004 (세션 3) |
| **단계** | TDD RED |
| **보고서 생성일** | 2026-06-25 |
| **목적** | 완성 격자에서 (2,2) 셀 변경으로 R2·C2 합 35 fail 케이스 RED 테스트 추가 |

---

## 1. 요약

- `/tdd-red` 커맨드로 **T2** fail 케이스 RED 테스트 1개를 `tests/test_validate_lines.py`에 추가했다.
- 완성 4×4 격자에서 **(2,2) 1-based** 셀 값을 **6→7**로 변경해 R2·C2 합이 **35**가 되도록 구성했다.
- `pytest` 실행 결과 **FAIL** 확인 — `validate_lines` 스텁이 `None` 반환 (`TypeError`). RED 목표 달성.
- `src/`는 수정하지 않았다.

## 2. 핵심 결정·산출물

| 구분 | 내용 |
|------|------|
| **테스트 함수** | `test_t2_fail_r2_c2_wrong_sum` |
| **입력 grid** | 마방진 기반 완성 격자, `grid[1][1]=7` (원본 6) |
| **기대 결과** | `status="fail"`, `failed_lines`에 `"R2"`, `"C2"` |
| **선 합 검증** | R2=5+7+12+11=35, C2=3+7+10+15=35 (나머지 선 34) |
| **pytest** | `FAILED` — `TypeError: 'NoneType' object is not subscriptable` |
| **변경 파일** | `tests/test_validate_lines.py` 만 |

## 3. 다음 단계

1. **GREEN**: `src/validate_lines.py`에 T2를 통과하는 최소 구현 (10선 합 검증, fail 시 `failed_lines` 보고).
2. **RED**: `incomplete` 케이스 (빈칸 `0` 존재) 테스트 추가.
3. **RED**: `pass` 케이스 (완성 마방진) 테스트 추가.

---

관련 Transcript: [Prompting/02.Export-Transcript.md](../Prompting/02.Export-Transcript.md)

*본 문서는 Report/02.REPORT.md — TDD RED T2 행·열 합 실패 테스트 추가 세션 보고입니다.*
