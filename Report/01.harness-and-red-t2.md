# 01.harness-and-red-t2 — 세션 보고

## 요약
- MagicSquare_1004 세션 3용 TDD Harness 골격(pyproject.toml, src/, tests/)을 구성했다.
- `.cursorrules` 초안과 Cursor 커맨드(`tdd-red`, `export-session`)를 작성했다.
- `/tdd-red`로 T2 fail 케이스(R2·C2 합 35) RED 테스트 1개를 추가하고 pytest FAIL을 확인했다.

## 변경 사항
| 파일 | 내용 |
|------|------|
| `pyproject.toml` | pytest 설정 (`pythonpath`, `testpaths`) |
| `src/__init__.py` | 빈 파일 |
| `src/validate_lines.py` | `def validate_lines(grid): ...` 시그니처만 |
| `tests/__init__.py` | 빈 파일 |
| `tests/test_validate_lines.py` | T2 RED 테스트 `test_t2_fail_r2_c2_wrong_sum` |
| `.cursorrules` | 도메인·API·TDD·AI 규칙 (56줄) |
| `.cursor/commands/tdd-red.md` | RED 단계 전용 커맨드 |
| `.cursor/commands/export-session.md` | Report·Prompting export 커맨드 |

## TDD 상태
- Phase: **red**
- pytest: **FAIL** — `validate_lines`가 `None` 반환 (`TypeError: 'NoneType' object is not subscriptable`)
- 테스트: `test_t2_fail_r2_c2_wrong_sum`
  - grid: (2,2) 1-based 값 6→7, R2·C2 합 35
  - 기대: `status="fail"`, `failed_lines`에 `"R2"`, `"C2"`

## 다음 단계
1. GREEN: `src/validate_lines.py`에 T2를 통과하는 최소 구현
2. RED: `incomplete`(빈칸 0) 케이스 테스트 추가
3. `.cursorrules` 보완 — 10선 좌표 매핑, 마법상수 34 SSOT, incomplete 시 `failed_lines=[]` 등
